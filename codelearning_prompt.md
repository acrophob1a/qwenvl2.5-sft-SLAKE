基于本项目的完整代码实现，详细拆解【预训练/微调/SFT/DPO】阶段的完整数据流。

要求：
1. 从**最原始的输入数据格式**开始，到**最终损失值输出**为止，覆盖全流程
2. 严格分为两个独立阶段，分别引用项目中对应的代码文件和函数：
   A. 数据预处理阶段（CPU上运行，对应Dataset/Tokenizer/Processor类）
   B. 模型前向传播阶段（GPU上运行，对应模型的forward方法）
3. 每一个操作步骤都必须包含以下5个要素，缺一不可：
   - 本项目中对应的**精确代码片段**（标注文件名和行号）
   - 输入数据的**精确shape**和**具体内容示例**
   - 输出数据的**精确shape**和**具体内容示例**
   - 每个维度的明确含义（如[B, T, C]中B=batch_size, T=序列长度, C=通道数）
   - 这一步操作在本项目中的核心目的
4. 使用我提供的**最小测试输入样本**贯穿全程，所有shape计算必须基于这个样本：
   原始输入 = {
       # 请在这里填写你项目中一个最小的、完整的输入样本
       # 例如VLM项目："images": [一张384x384的RGB图片], "texts": ["描述这张图片"], "labels": ["一只猫"]
       # 例如LLM项目："texts": ["今天天气很好"], "labels": ["今天天气很好"]
       # 例如视频项目："videos": [一段8帧224x224的视频], "captions": ["一只狗在跑"]
   }
5. 对于多模态/多输入模型，必须：
   - 先分别拆解每个模态/输入的独立数据流
   - 再明确说明多模态融合发生在代码的哪一行，融合前后的shape分别是什么
   - 说明融合的具体方式（拼接/交叉注意力/加权求和等）
6. 模型前向传播阶段，必须按核心模块拆解shape变化：
   - 从输入张量进入forward方法开始
   - 经过每一个核心模块（嵌入层/注意力层/FFN层/融合层/输出头）
   - 直到最终logits和损失计算完成
7. 不要跳过任何中间步骤，包括：
   - 特殊标记添加、padding、truncation
   - 任何形式的归一化、标准化
   - 注意力掩码/因果掩码/损失掩码的生成
   - 标签的任何处理（偏移、掩码等）
8. 最后：
   - 用纯文本画一个完整的shape变化流程图
   - 指出本项目代码中任何可能导致shape不匹配的潜在问题